<UserControl x:Class="JTSA.Panels.CategoryPanel"
             xmlns:panels="clr-namespace:JTSA.Panels"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#FF404040">

    <DockPanel>
        <Grid>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>


                <Grid Height="Auto" Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="24"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0">
                        <Button DockPanel.Dock="Right" Content="カテゴリ追加" Width="90" Height="22"
                                Margin="8,0,0,2" Click="CategoryAddButton_Click"/>
                        <Label Content="※ダブルクリックすると選択カテゴリを変更" Foreground="White"/>
                    </DockPanel>

                    <ListBox ItemsSource="{Binding CategoryFormList}" d:ItemsSource="{d:SampleData ItemCount=3}" Grid.Row="1" Background="#FF404040"
                x:Name="CategoryListBox" Height="Auto" Width="Auto" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto"
                MouseDoubleClick="CategoryListBox_MouseDoubleClick">

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <Grid Margin="8,0,8,0">
                                    <StackPanel Margin="0,0,28,0">
                                        <Grid Margin="0" Height="Auto">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <Image Source="{Binding BoxArtUrl}" Grid.Column="0" Width="36" Height="48" Margin="0,0,8,0"/>

                                            <Grid Margin="0" Height="Auto" Grid.Column="1" >
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <TextBlock Grid.Row="0" Text="{Binding DisplayName}" Width="Auto" Foreground="White" FontSize="16" TextWrapping="Wrap"></TextBlock>

                                                <Grid Grid.Row="1" Grid.Column="1" Margin="0, 0, 0, 0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Label Grid.Column="0" Margin="0" VerticalAlignment="Bottom" Foreground="White" FontSize="10">SteamURL：</Label>
                                                    <TextBox Grid.Column="1" Width="Auto" Name="SteamURLTextBox" Text="{Binding SteamUrl}"></TextBox>
                                                    <Button
                                                        Grid.Column="2" Grid.Row="1"  Content="URL更新" Width="64" Height="20" Margin="8, 0, 0, 0" VerticalAlignment="Center"
                                                        Click="SteamURLUpdateButton_Click"/>
                                                </Grid>

                                                <Grid Grid.Row="2" Margin="0,4,0,0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Label Grid.Column="0" Margin="0" VerticalAlignment="Center"
                                                           Foreground="White" FontSize="10" Content="日本語名："/>
                                                    <TextBox Grid.Column="1"
                                                             Text="{Binding JapaneseDisplayName, Mode=TwoWay}"
                                                             ToolTip="編集後、フォーカスを外すと保存されます"
                                                             LostKeyboardFocus="JapaneseNameTextBox_LostKeyboardFocus"/>
                                                    <Button Grid.Column="2" Content="取得" Width="64" Height="20"
                                                            Margin="8,0,0,0" VerticalAlignment="Center"
                                                            ToolTip="IGDBから日本向けカテゴリ名を再取得します"
                                                            Click="JapaneseNameFetchButton_Click"/>
                                                </Grid>

                                                <!-- カテゴリに紐づくチャンネルポイントプリセット（未設定なら切り替えを行わない） -->
                                                <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0, 4, 0, 0">
                                                    <Label Margin="0" VerticalAlignment="Center" Foreground="White" FontSize="10">CPプリセット：</Label>
                                                    <ComboBox Width="180" Height="20" VerticalAlignment="Center"
                                                              ItemsSource="{Binding DataContext.ChannelPointPresetFormList, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                              DisplayMemberPath="PresetName"
                                                              SelectedValuePath="PresetId"
                                                              SelectedValue="{Binding ChannelPointPresetId, Mode=TwoWay}"
                                                              SelectionChanged="CategoryPresetComboBox_SelectionChanged"
                                                              ToolTip="このカテゴリを設定したときに自動適用するプリセット。「（自動適用しない）」を選ぶと何も変更しません。"/>
                                                    <Button Content="OBSキャプチャ先" Width="110" Height="20" Margin="8,0,0,0"
                                                            VerticalAlignment="Center"
                                                            ToolTip="このカテゴリのOBSキャプチャ先設定を開きます"
                                                            Click="OpenObsCaptureDestinationButton_Click"/>
                                                </StackPanel>
                                            </Grid>
                                        </Grid>
                                        <Grid Margin="0" Height="Auto">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <StackPanel Orientation="Horizontal" Grid.Column="0"  Grid.Row="1" Margin="8, 0, 0, 0" >
                                                <Label  Margin="0" VerticalAlignment="Bottom" Foreground="White" FontSize="10">最終使用日：</Label>
                                                <TextBlock Text="{Binding LastUsedDate}"  Name="LastUsedDate" Height="20" VerticalAlignment="Bottom" Margin="0" Foreground="White"></TextBlock>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                    <Button Width="20" Height="20"
                                            Margin="0,2,0,0" Padding="0"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Top"
                                            Background="Transparent"
                                            Foreground="#FFFF6F78"
                                            BorderThickness="0"
                                            FontSize="16"
                                            Click="CategoryDeleteButton_Click">
                                        <TextBlock Text="×"
                                                   Padding="0,0,0,3"
                                                   TextAlignment="Center"
                                                   Foreground="#FFFF6F78"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Button>
                                    </Grid>

                                    <Border Width="Auto" Height="2" Background="White"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Grid>

        </Grid>
    </DockPanel>
</UserControl>
