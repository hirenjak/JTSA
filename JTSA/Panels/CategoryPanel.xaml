<UserControl x:Class="JTSA.Panels.CategoryPanel"
             xmlns:panels="clr-namespace:JTSA.Panels"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#FF404040">

    <DockPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>


                <Grid Height="Auto" Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="24"/>
                        <RowDefinition Height="24"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0" Content="※ダブルクリックすると選択カテゴリを変更" Foreground="White"/>

                    <Label Grid.Row="1" Content="※プレイ中追加をクリックすると右タブ画面のOBS表示用のプレイ中ゲーム画面にカテゴリを追加します。" Foreground="White"/>

                    <ListBox ItemsSource="{Binding CategoryFormList}" d:ItemsSource="{d:SampleData ItemCount=3}" Grid.Row="2" Background="#FF404040"
                x:Name="CategoryListBox" Height="Auto" Width="Auto" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto"
                MouseDoubleClick="CategoryListBox_MouseDoubleClick">

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <StackPanel Margin="8, 0, 8, 0">
                                        <Grid Margin="0" Height="Auto">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <Grid Margin="0, 0, 8, 0" Height="Auto" Grid.Column="0" >
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <Button Grid.Row="0" Content="カテゴリ設定" Width="72" Height="20" Margin="0, 0, 0, 4" VerticalAlignment="Center" Click="CategorySetButton_Click"/>
                                                <Button Grid.Row="1" Content="プレイ中追加" Width="72" Height="20" Margin="0, 0, 0, 0" VerticalAlignment="Center" Click="PlayCategoryAddButton_Click"/>
                                            </Grid>
                                            <Image Source="{Binding BoxArtUrl}" Grid.Column="1" Width="36" Height="48" Margin="0,0,8,0"/>

                                            <Grid Margin="0" Height="Auto" Grid.Column="2" >
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>
                                                <TextBlock Grid.Row="0" Text="{Binding DisplayName}" Width="Auto" Foreground="White" FontSize="16" TextWrapping="Wrap"></TextBlock>

                                                <Grid Grid.Row="1" Grid.Column="1" Margin="0, 0, 0, 0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Label Grid.Column="0" Margin="0" VerticalAlignment="Bottom" Foreground="White" FontSize="10">SteamURL：</Label>
                                                    <TextBox Grid.Column="1" Width="Auto" Name="SteamURLTextBox" Text="{Binding SteamUrl}"></TextBox>
                                                    <Button
                                                        Grid.Column="2" Grid.Row="1"  Content="URL更新" Width="64" Height="20" Margin="8, 0, 0, 0" VerticalAlignment="Center" 
                                                        Click="SteamURLUpdateButton_Click"/>
                                                </Grid>
                                            </Grid>
                                        </Grid>
                                        <Grid Margin="0" Height="Auto">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <StackPanel Orientation="Horizontal" Grid.Column="0"  Grid.Row="1" Margin="8, 0, 0, 0" >
                                                <Label  Margin="0" VerticalAlignment="Bottom" Foreground="White" FontSize="10">最終使用日：</Label>
                                                <TextBlock Text="{Binding LastUsedDate}"  Name="LastUsedDate" Height="20" VerticalAlignment="Bottom" Margin="0" Foreground="White"></TextBlock>
                                            </StackPanel>
                                            <Button Grid.Column="2" Grid.Row="1"  Content="削除" Width="64" Height="20" Margin="8, 0, 0, 0" VerticalAlignment="Center" Click="CategoryDeleteButton_Click"/>
                                        </Grid>
                                    </StackPanel>

                                    <Border Width="Auto" Height="2" Background="White"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Grid>

            <panels:CategorySearchPanel 
                x:Name="CategorySearchPanel"
                Grid.Column="1"/>
        </Grid>
    </DockPanel>
</UserControl>
