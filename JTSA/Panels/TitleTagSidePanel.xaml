<UserControl x:Class="JTSA.Panels.TitleTagSidePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DockPanel>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ListBox Background="#FF404040"
                    x:Name="TitleTagListBox"
                    Height="Auto" Width="Auto"
                    HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto"
                    SelectionChanged="TitleTagListBox_SelectionChanged" PreviewMouseDown="TitleTagListBox_PreviewMouseDown">

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <Grid Margin="8,0,8,0">
                            <StackPanel Margin="0,0,28,0">
                                <TextBlock Text="{Binding DisplayName}" Width="Auto" Foreground="White" FontSize="14" TextWrapping="Wrap">
                                </TextBlock>
                                <Grid Margin="0" Height="Auto">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="{Binding Placeholder}" Width="Auto" Grid.Column="0" Foreground="White" FontSize="10" VerticalAlignment="Center"></TextBlock>
                                    <StackPanel Orientation="Horizontal" Grid.Column="1" Margin="8, 0, 0, 0" >
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSystem}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <Label Margin="0" VerticalAlignment="Bottom" Foreground="White" FontSize="10">最終使用日：</Label>
                                        <TextBlock Text="{Binding LastUsedDate}"  Name="LastUsedDate" Height="20" VerticalAlignment="Bottom" Margin="0" Foreground="White"></TextBlock>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                                    <Button Width="20" Height="20"
                                            Margin="0,2,0,0" Padding="0"
                                            HorizontalAlignment="Right" VerticalAlignment="Top"
                                            Background="Transparent" Foreground="#FFFF6F78"
                                            BorderThickness="0" FontSize="16"
                                            Click="TitleTagDeleteButton_Click">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="Foreground" Value="#FFFF6F78"/>
                                                <Setter Property="BorderThickness" Value="0"/>
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSystem}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                        <TextBlock Text="×"
                                                   Padding="0,0,0,3"
                                                   TextAlignment="Center"
                                                   Foreground="#FFFF6F78"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Button>
                            </Grid>

                            <Border Width="Auto" Height="2" Background="White"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </DockPanel>
</UserControl>
