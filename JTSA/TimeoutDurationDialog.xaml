<Window x:Class="JTSA.TimeoutDurationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Twitchタイムアウト"
        Width="360" Height="205"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#FF303030"
        ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TargetUserTextBlock"
                   Foreground="White" FontSize="14" TextWrapping="Wrap"/>
        <TextBlock Grid.Row="1" Margin="0,14,0,5"
                   Foreground="LightGray" Text="タイムアウトする秒数"/>
        <TextBox x:Name="DurationTextBox" Grid.Row="2"
                 Height="28" Padding="6,3" Text="600"
                 PreviewTextInput="DurationTextBox_PreviewTextInput"
                 KeyDown="DurationTextBox_KeyDown"/>
        <TextBlock Grid.Row="3" Margin="0,5,0,0"
                   Foreground="DarkGray" FontSize="11"
                   Text="1〜1,209,600秒（最大14日）"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="キャンセル" Width="82" Height="28" Margin="0,0,8,0"
                    IsCancel="True"/>
            <Button Content="タイムアウト" Width="96" Height="28"
                    IsDefault="True" Click="ConfirmButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
